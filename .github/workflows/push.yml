name: Frog Desk Manual

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Manual only
        run: |
          echo "Automatic Frog Desk pushes are disabled while v5 is being tested."
          echo "The Star file chooses a random turn each time Pixlet renders it."
