name: Render and Push Frog Desk

on:
  workflow_dispatch:

jobs:
  push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Disabled scheduled push
        run: |
          echo "Scheduled Frog Desk pushing is disabled."
          echo "Use manual pushes while the app is being built."
